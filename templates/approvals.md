# 保护区改动审批记录

> 由 `tools/shared/project_guard.py --approve <文件> <理由>` 自动追加。
> 保护区文件（startup_*.s / 向量表 / *.sct / *.ld / *.icf / system_*.c）改动必须人工审批，否则 `/ea verify` 阻塞。

## 审批格式
- <ISO时间> | `<文件相对路径>` | <理由>

## 历史记录

（无）
